# CLAUDE.md｜今日热搜榜 · Claude Code 协作说明

> 版本：v0.1  
> 日期：2026-05-28  
> 默认角色：代码审查、排错与明确指派的小范围修复

---

## 1. 介入前先读取

处理本仓库任何问题前，请先阅读：

1. `PRD.md`：产品范围与验收标准；
2. `TECH_DESIGN.md`：架构、接口、数据模型与部署方案；
3. `AGENTS.md`：Codex 开发规则与禁止扩展事项。

如用户当轮给出更具体的新要求，以用户最新要求为准，并指出是否需要同步更新上述文档。

---

## 2. 已确定且不得自行改变的架构

```text
client/：React + TypeScript + Vite + CSS Modules → Vercel
server/：Node.js + Express + TypeScript + 内存 Map 缓存 → Railway
```

固定范围：

- 三个平台：微博、知乎、B站；
- API：`GET /api/health`、`GET /api/hot/:source`、`GET /api/hot`；
- 缓存：服务端内存缓存，默认 `CACHE_TTL=600`；
- Day 7–12 可以使用 Mock；线上版本必须使用真实数据；
- 页面支持 loading / empty / error / success 及单卡重试。

除非用户明确要求，不要将项目改为 Next.js、Tailwind、Redis/KV、数据库、AI 摘要、推送、更多平台或新的部署架构。

---

## 3. 默认协作角色

Codex 负责主开发。Claude Code 默认执行以下工作：

- 审查 Codex 当轮新增或修改的代码；
- 定位运行、构建、接口、缓存、CORS 或部署错误；
- 检查真实数据 Provider 的字段解析与错误隔离；
- 在用户明确要求修复时，只改动解决问题所需的最小文件范围；
- 给出可验证的测试或复现步骤。

没有明确修复请求时，优先输出审查结果和建议，不主动重构整个项目。

---

## 4. 审查重点清单

### 前端

- 组件是否只渲染统一模型，不直接访问外部平台域名；
- 是否使用 CSS Modules，而非新增无关依赖；
- loading、empty、error、success 和重试状态是否正确；
- 外链是否包含 `target="_blank"` 与 `rel="noopener noreferrer"`；
- 桌面三列与手机单列是否正常。

### 后端

- 路由、Service、Provider、Cache 职责是否清楚；
- 外部数据是否校验后再转换，不直接透传；
- 单平台失败是否不会拖垮聚合接口；
- 内存缓存是否按平台隔离、TTL 是否生效；
- CORS 与环境变量是否符合本地/生产要求；
- 是否存在硬编码 token、Cookie、敏感 URL 或规避平台限制的逻辑。

### 发布前

- `client` 与 `server` 是否均可构建；
- README 是否包含启动、环境变量、数据来源、缓存与部署说明；
- Vercel 与 Railway 环境变量是否匹配；
- 页面是否包含非官方与学习项目说明；
- 生产环境是否仍引用 Mock 数据。

---

## 5. 排错输出格式

诊断问题时，请按以下结构回答：

```text
现象复述：
- ...

最可能原因：
- ...

定位步骤：
1. ...
2. ...

建议修改文件：
- ...

修改后验证：
- 命令 / 浏览器检查步骤 ...

是否超出当前范围：
- 是 / 否；如是，说明原因并先等待用户决策
```

如果已被要求直接修复，完成修改后列出修改文件、执行命令、实际结果和尚未验证的风险。
