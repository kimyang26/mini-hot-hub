# PRD.md｜今日热搜榜：多平台热点聚合网站

> 阶段：Step 2 — 产品需求文档（PRD）  
> 版本：v0.4（课程兼容架构同步版）  
> 日期：2026-05-28  
> 产品发起人：Kim  
> 关联文档：`RESEARCH.md`、`TECH_DESIGN.md v0.4`、`AGENTS.md v0.3`

---

## 0. 本版同步说明

本版用于与 21 天 Vibe Coding 教程的开发路径以及最新技术方案统一口径。产品方向不变，实施路径明确为：

```text
client/：React + TypeScript + Vite + CSS Modules → Vercel
server/：Node.js + Express + TypeScript + 内存缓存 → Railway
```

本次同步后的关键变化：

- MVP 仍聚焦 **微博、知乎、B站** 三个平台的真实热榜首页；
- 允许 Day 7–12 使用 Mock 完成页面、接口与缓存联调，线上版本不得以 Mock 代替真实数据；
- 服务端内存缓存、`GET /api/hot/:source` 与 `GET /api/hot` 进入本期实施范围；
- 单卡重试属于课程上线前体验增强；全页刷新可作为 Day 18 优化项实现；
- Redis / KV、AI 每日摘要、自动推送及新增平台仍不进入本期 MVP。

---

## 1. 产品概述

### 1.1 产品名称

**今日热搜榜**

### 1.2 一句话说明

一个可公开访问的轻量热搜聚合网站：首页以卡片网格展示微博、知乎与 B站当前热门内容，让用户一页快速扫完热点，并跳转原平台了解详情。

### 1.3 产品价值

热点信息分散在多个 App 与页面中。用户如果只想快速了解“今天大家在关注什么”，需要频繁切换平台。今日热搜榜将三个典型内容平台的榜单集中呈现，提供一个简洁、可信、易分享的信息入口。

### 1.4 本期目标

本期交付一个可上线、可分享、可演示开发过程的 MVP：

- 首页展示微博热搜、知乎热榜、B站热门三张卡片；
- 线上展示真实可点击的热榜数据；
- 数据具备约 5–10 分钟更新节奏，避免频繁请求来源；
- 某个平台失败时，其余平台仍能浏览；
- 网站可通过 HTTPS 公网链接访问；
- 开发过程可覆盖前端、后端、缓存、联调与部署等课程任务。

---

## 2. 目标用户

| 用户类型 | 使用场景 | MVP 提供的价值 |
|---|---|---|
| 想快速了解热点的普通访客 | 工作或休息间隙快速扫一眼热点 | 一页浏览三个平台，点击查看原始内容 |
| 内容创作者 / 选题观察者 | 快速发现不同平台的讨论方向 | 保留排名、热度信息（如有）及原链接 |
| Vibe Coding 学习者 / 作品访问者 | 查看一个完整上线的学习作品 | 能看到真实数据、异常处理、响应式与部署结果 |

暂不优先服务：需要专业舆情监控、历史趋势分析、数据导出或企业级稳定性承诺的用户。

---

## 3. 产品范围

### 3.1 P0｜核心 MVP 必须实现

| 功能 | 说明 | 验收表现 |
|---|---|---|
| 三平台卡片首页 | 微博、知乎、B站三张独立卡片 | 首页打开即可看到三张卡片 |
| Top 10 热榜列表 | 每个平台展示最多 10 条热点 | 每条包含排名、标题与可点击链接；热度可选 |
| 真实数据 | 线上版本使用真实可访问数据源 | 发布版本不依赖 Mock 数据 |
| 状态反馈 | loading、empty、error 与成功列表展示 | 单个平台异常不会导致页面白屏 |
| 更新时间与缓存说明 | 告知用户数据新鲜度 | 卡片显示更新时间，页脚说明更新频率约 10 分钟 |
| 响应式页面 | 桌面与手机均可阅读 | 桌面三列，手机单列 |
| 页脚合规声明 | 说明非官方、学习用途、版权归属 | 页面底部可见 |
| 公网访问 | 网站可分享给他人访问 | HTTPS 链接打开正常 |

### 3.2 P0.5｜课程交付增强（上线前完成或择优完成）

这些功能不改变产品定位，服务于教程 Day 16–18 的容错验证与体验完善：

| 功能 | 优先级 | 说明 |
|---|---:|---|
| 单卡重试 | 必做增强 | 某卡片失败后可重新请求该平台接口，用于验证故障恢复 |
| 全页刷新按钮 | 可选增强 | 重新发起聚合请求；仍受缓存策略约束，不承诺强制获取最新数据 |
| 相对更新时间 | 推荐增强 | 如“更新于 3 分钟前”，提高可理解性 |
| hover、favicon、移动端间距优化 | 推荐增强 | 提升完成品观感 |

### 3.3 P1｜课程完成后的产品增强

| 功能 | 价值 |
|---|---|
| 抖音、YouTube 接入 | 扩大信息覆盖，验证国内外热点同屏 |
| 最近成功数据回退 | 外部来源短时异常时提升浏览稳定性 |
| 页面内每日 10 条 AI 摘要 | 验证从“榜单聚合”到“重点提炼”的差异化价值 |
| 平台筛选 | 让用户减少无关信息 |

### 3.4 P2｜后续探索

- 小红书、X、Reddit、即刻等平台接入；
- 每日自动摘要推送；
- 历史摘要归档；
- 关键词过滤与分享卡片；
- 简单平台管理后台。

### 3.5 本期明确不做

- 登录、注册、订阅、收藏或付费功能；
- 跨平台综合热度排名；
- 情绪 / 舆情分析、评论抓取与分析；
- Redis / KV / 数据库、历史榜单持久化；
- AI 摘要或自动推送；
- 秒级实时刷新；
- 为获取数据而进行 Cookie 绕过、模拟登录或高风险反爬对抗。

---

## 4. 核心用户流程

### 4.1 正常浏览流程

```text
用户打开公网链接
  ↓
页面加载三张平台卡片
  ↓
后端返回微博 / 知乎 / B站榜单数据
  ↓
用户浏览排名、标题、热度（如有）与更新时间
  ↓
点击条目，在新标签页打开原平台内容
```

### 4.2 单平台失败流程

```text
某个平台数据暂时不可用
  ↓
该平台卡片展示获取失败提示与重试入口
  ↓
其他平台卡片继续展示正常内容
```

### 4.3 数据更新流程

```text
用户访问页面
  ↓
后端优先读取有效缓存
  ↓
缓存未命中或过期时获取外部数据并重新缓存
  ↓
页面显示该批数据的更新时间
```

---

## 5. 核心功能需求

### 5.1 首页卡片网格

首页包含：

1. `Header`：站点名称「今日热搜榜」、一句简介；
2. `HotGrid`：三张独立热榜卡片；
3. `Footer`：更新频率、非官方与学习项目说明。

平台范围：

| source | 展示名称 | 榜单名称 | 本期要求 |
|---|---|---|---|
| `weibo` | 微博 | 热搜榜 | 真实数据 |
| `zhihu` | 知乎 | 热榜 | 真实数据 |
| `bilibili` | B站 | 热门 | 真实数据 |

### 5.2 榜单条目

每条热点内容包含：

| 字段 | 必需 | 说明 |
|---|---:|---|
| `rank` | 是 | 该榜单中的顺序或排名 |
| `title` | 是 | 热点标题，保持原意 |
| `url` | 是 | 原平台可访问链接 |
| `heat` | 否 | 上游提供时展示，不跨平台比较 |

交互规则：

- 标题点击后在新标签页打开原链接；
- 外链需使用安全属性；
- 不提供站内详情页或内容转载页；
- 热度缺失时不展示空占位。

### 5.3 平台卡片状态

平台层级数据包含：

| 字段 | 必需 | 说明 |
|---|---:|---|
| `source` | 是 | `weibo` / `zhihu` / `bilibili` |
| `sourceName` | 是 | 平台中文展示名称 |
| `listName` | 是 | 榜单展示名称 |
| `status` | 是 | `success` / `empty` / `error` |
| `updatedAt` | 成功或缓存数据时是 | ISO 8601 更新时间 |
| `items` | 是 | 热点条目数组 |
| `message` | 错误或空状态时可用 | 面向用户的状态提示 |

展示规则：

| 状态 | 页面表现 | 推荐文案 |
|---|---|---|
| loading | 卡片加载占位 | 正在获取热榜… |
| success | 展示列表与更新时间 | 更新于 HH:mm |
| empty | 保留卡片，列表区域为空状态 | 当前暂无可展示内容 |
| error | 保留卡片，显示重试入口 | 暂时获取失败，请稍后再试 |

说明：`loading` 属于前端请求过程，不属于后端平台状态枚举。

### 5.4 数据接口与异常隔离的产品要求

页面所需数据必须通过本站后端获得，浏览器不直接请求外部热榜数据源。

本期需支持：

- 聚合请求：一次获取三个平台的数据；
- 单平台请求：供渐进接入、排错与单卡重试使用；
- 任一平台失败时，其余平台仍可返回并展示；
- 数据结构统一，不让页面分别理解不同来源的原始字段。

### 5.5 缓存与更新时间

本期热榜更新目标为 **约 10 分钟级别**，具体要求：

- 每个平台独立缓存，默认 TTL 为 600 秒；
- 缓存期内重复浏览不应频繁触发外部数据请求；
- 缓存命中时，该平台更新时间保持一致；
- 用户触发页面刷新不等同于强制绕过缓存；
- 本期不要求在上游失败后继续展示历史成功数据。

### 5.6 合规声明

页脚至少表达以下含义：

> 本站为非官方热点聚合学习项目，榜单内容来源于各平台公开或可访问数据，内容与版权归原平台及原作者所有。信息仅供浏览参考，请以原平台内容为准。数据约每 10 分钟更新一次。

---

## 6. 界面设计要求

### 6.1 视觉方向

关键词：**清爽、轻量、易扫读、像一页每日信息面板**。

- 使用浅色背景与明确卡片分区；
- 平台色只作小面积识别点缀；
- 排名 1–3 可轻度强调；
- 标题优先级高于热度与更新时间；
- 不引入复杂导航、大面积装饰图或影响阅读的动画。

### 6.2 布局要求

| 终端 | 布局 |
|---|---|
| 桌面端 | 三列卡片同屏展示 |
| 中等屏幕 | 两列或自适应布局 |
| 手机端 | 单列卡片，点击区域清晰 |

### 6.3 卡片细节

- 卡片顶部：平台名、榜单名、可识别的平台标记；
- 卡片中部：Top 10 列表；
- 卡片底部：更新时间或状态提示；
- 标题最多两行，超出截断；
- 异常状态不改变整体页面骨架；
- 重试按钮只在错误态展示。

---

## 7. 分阶段交付映射

| 开发阶段 | 数据形态 | 页面 / 功能产出 |
|---|---|---|
| Day 7–9 | 前端本地 Mock | 三卡首页、响应式、loading / empty / error UI |
| Day 10–11 | Express Mock API | 前端数据全部来自后端接口 |
| Day 12 | Express Mock + 内存缓存 | 完成缓存命中验证 |
| Day 13–15 | 真实数据 Provider | 三平台切换为真实热榜 |
| Day 16–18 | 真实数据 | 测试、错误恢复、体验优化、构建 |
| Day 19–20 | 线上真实数据 | Vercel + Railway 公网访问 |
| Day 21 | 线上版本 | 总结、反馈与小修复 |

Mock 使用规则：Mock 只用于开发阶段与测试；发布版本必须接入真实数据，无法稳定接入的平台应如实说明现状与风险，不得伪装为真实热榜。

---

## 8. MVP 验收标准

本期交付通过需满足：

1. 网站可通过 HTTPS 公网链接打开；
2. 首页展示微博、知乎、B站三张独立卡片；
3. 线上版本使用真实数据，不依赖 Mock；
4. 正常状态下每个平台展示 Top 10 可点击条目，若平台当天有效条目不足应如实记录；
5. 每条内容包含排名、标题、原链接，热度可选；
6. 页面具备 loading、empty、error 与成功显示状态；
7. 任一平台失败时，其余两个平台仍能正常展示；
8. 服务端缓存默认 600 秒，重复访问能验证缓存命中；
9. 错误卡片具备重试能力；
10. 桌面端三列、手机端单列，页面可读且无横向溢出；
11. 页脚包含非官方、学习项目、来源和更新频率说明；
12. 前端部署至 Vercel、后端部署至 Railway，线上前后端可正常联通；
13. 本期没有加入 AI 摘要、推送、登录、历史榜或更多平台等超范围功能。

---

## 9. 与技术实施的同步约束

Codex 与 Claude Code 执行本项目时，产品范围以本 PRD 为准，具体实现以 `TECH_DESIGN.md v0.4` 与 `AGENTS.md v0.3` 为准：

- 采用 `client/` 与 `server/` 前后端分离结构；
- 前端为 React + TypeScript + Vite + CSS Modules；
- 后端为 Node.js + Express + TypeScript；
- 开发期允许 Mock，发布版本必须使用真实数据；
- 本期 API 包含聚合接口与单平台接口；
- 缓存采用 Express 内存缓存，默认 TTL 600 秒；
- 前端部署 Vercel，后端部署 Railway；
- 每日 10 条摘要和自动推送进入课程完成后的下一版本。

---

## 10. PRD 总结

本期目标不是把所有热榜平台一次做全，而是完成一个**真实、可访问、可演示完整开发链路**的三平台热点首页：先用 Mock 快速跑通页面，再通过 Express 接入真实数据与缓存，最终发布为可分享的线上作品。
